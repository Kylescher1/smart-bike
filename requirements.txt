# Core Scientific Computing
numpy==1.26.4
opencv-contrib-python==4.8.1.78
scipy
matplotlib  # For plotting and visualization (used by SpinningLidar)
# Optional: For debug plotting (requires Qt backend)
# pyqtgraph  # For real-time plotting (used by SpinningLidar.start_debug_plot())
# PyQt5 or PySide2  # Qt backend required by pyqtgraph
# Install system package: sudo apt install python3-pyqt5
# Or install via pip: pip install pyqtgraph PyQt5 (requires Qt dev libraries)
dill  # For serializing configuration and functions
numpy-quaternion  # For quaternion math (used in sensor configuration) - install via: pip install numpy-quaternion

# Sensor Interfaces
pyserial==3.5  # For LIDAR and other serial devices
rplidarc1  # For RPLidar C1 laser scanner
smbus2==0.4.2  # I2C interface for IMU and TOF

# Logging and Telemetry
python-json-logger==2.0.7
pyzmq  # For pub/sub communication (ZMQ messaging)

# Optional: ROS2 Bridge (future)
# rclpy==0.14.1

# Development and Testing
pytest==7.3.1
mypy==1.3.0
black==23.3.0

# Performance Profiling
memory-profiler==0.60.0

Flask==3.1.2

ultralytics
